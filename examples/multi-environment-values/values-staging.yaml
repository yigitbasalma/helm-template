# Staging ortamı — sadece farklı olan key'ler
# Kullanım: helm install myapp . -f values.yaml -f values-staging.yaml

replicaCount: 2

environmentsMap:
  DATABASE_HOST: "db-staging.internal"
  LOG_LEVEL: "info"
  APP_ENV: "staging"
  CORS_ORIGINS: "https://staging.mycompany.com"